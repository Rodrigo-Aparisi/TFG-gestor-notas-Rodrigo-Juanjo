# Variables para el contenedor Docker de PostgreSQL.
# Copia este archivo a .env.docker y ajusta los valores.
# El archivo .env.docker real NO se versiona.
# docker compose --env-file .env.docker up -d

DB_USER=olympus
DB_PASSWORD=cambia_esta_password
DB_NAME=olympus_scribe
DB_PORT=5432
